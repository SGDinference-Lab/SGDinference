% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgdi.R
\name{sgdi}
\alias{sgdi}
\title{Averaged SGD and its Inference via Random Scaling}
\usage{
sgdi(
  x,
  y,
  z = NA,
  gamma_0 = 1,
  alpha = 0.667,
  burn = 1,
  model = "lm",
  inference = "rs",
  bt_start = NULL,
  path_output = NULL,
  qt = 0.5,
  studentize = TRUE,
  intercept = TRUE
)
}
\arguments{
\item{x}{numeric. (n x p) matrix of regressors. Should not include 1 (the intercept)}

\item{y}{numeric}

\item{z}{numeric. (n x q) matrix of instruments used for \code{"tsls"} (Two-Stage Least Squares)}

\item{gamma_0}{numeric}

\item{alpha}{numeric}

\item{burn}{numeric}

\item{model}{character specifying the model to be used: \code{"lm"} (linear
model)}

\item{inference}{character specifying the inference method. Default is "rs" (random scaling)}

\item{bt_start}{numeric}

\item{path_output}{numeric specifying the sequence that print out the output paths}

\item{qt}{numeric. Quantile. Default is 0.5.}

\item{studentize}{logical. Studentize regressors. Default is TRUE}

\item{intercept}{logical. Use the intercept term for regressors. Default is TRUE}
}
\value{
#' An object of class \code{"sgdi"}, which is a list containing the following
components:
}
\description{
Compute the averaged SGD estimator and the confidence intervals via random scaling method.
}
\examples{
n = 1e05
p = 5
bt0 = rep(5,p)
x = matrix(rnorm(n*(p-1)), n, (p-1))
y = cbind(1,x) \%*\% bt0 + rnorm(n)
sgdi.out = sgdi(x,y)
}
